A chaque pages bien diviser les cas de provenance (par exemple dans editeur la barre de recherche envoie un nomEditeur alors que infoEditeur utilise un num donc il faut faire la conversion, alors que le bouton info envoie directement le num )et chercher les infos utiles tel que numEditeur ou numContact ...


Il faut faire une rubrique réservations qui met les infos principales et renvoie vers l’editeur et les jeux (faire une liste des jeux avec à côté un bouton qui mène vers le descriptif du jeux.





Dans infoEditeur mettre toutes ses réservations et à chaque résa mettre les jeux et le logement ou non affiliés


ajoutReservation :  


Dans info résa afficher les jeux c’est difficile avec la date de sortie souvent on a pas bien fait la date et ca donne 0000-00-00 donc pourquoi pas juste mettre du texte ? Pareil pour duree partie 


Pour les catégories ca affiche uniquement le code catégories, il faudrait une fonction qui affiche le nom de la caté


Dans jeux id editeur est en id il faudrait le mettre sous forme de nom editeur
